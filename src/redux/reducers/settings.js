export default (state = {
    /**
     * apiHost,
     * status -> order status: { "submitted": { id, label}, ... }
     * models -> vehicle models: { "mvp": { id, label}, ... }
     */
}) => (state);